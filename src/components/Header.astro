---
import {
	SignInButton,
	SignedIn,
	SignedOut,
	UserButton,
} from "@clerk/astro/components";
---

<header class="w-full py-6 border border-b-gray-600/40">
  <nav class="max-w-screen-xl mx-auto">
    <SignedOut>
      <SignInButton mode="modal" />
    </SignedOut>
    <SignedIn>
      <UserButton />
    </SignedIn>
  </nav>
</header>
